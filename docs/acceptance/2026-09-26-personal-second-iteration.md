# 个人助理第二轮实现、验收与 Review

日期：2026-09-26。依据：[第二轮竞品对标](../competitor-benchmark-2026-09-26.md)和[统一路线图](../personal-agent-roadmap.md)。本记录只描述本地代码和模拟验收；未部署 EC2、未执行付费模型任务、未读写真实邮箱或日历。

## 本轮交付

- 个人空间增加“今日”“资料”和按空间显示的审批；今日汇总已有待处理/运行状态、最近文件及对话，文件可直达预览、下载和继续交办。工作项目的审批不再出现在个人页。
- 任务建议按当前服务状态标识邮箱/日历，缺失时可直达连接面板；上游目录受限时标记状态待确认，不把未知误判为未连接。选择任务仅填草稿。
- 共用运行时和独立 worker 都允许用户主动上传个人附件；专用模式经第二条 Unix socket 由低权限 worker 校验、存取，主服务不直接读取其私有目录。服务端限定大小、路径与数量，个人模型默认只读。资料接口只列个人工作区的普通文件，隐藏配置路径、拒绝符号链接，并以受限流式响应下载。个人上传副本不随会话删除；资料页显式确认删除且在仍被会话引用时拒绝。
- 个人事实由用户明确添加、修改和删除，存于独立状态文件；新建或恢复个人线程时提供当前事实。删除不会修改旧对话。设置页把账号、服务、事实和通知置于基础设施诊断之前。
- 用量视图修复筛选旧响应覆盖新响应，明确加载/失败/未知用量与浏览器时区；24 小时图包含首尾部分小时，并提供可读分时表、请求到运行模型及 token 的下钻、只读 JSON 导出，不导出令牌或输入正文。
- 独立调用方增加最小参考客户端：稳定业务幂等键提交、带身份的结果轮询、限流退避及显式取消。只用假 HTTP 服务端验收，未运行真实自动化。
- 自动化运行事件持久保存递增游标，现有结果接口可按 `after` 读取最近事件并标识裁剪缺口；对外只返回时间、类型与序号。没有把 JSON 状态文件误称为事务队列。
- 手机个人对话收起重复设置区，改善触控目标、面板焦点与 Escape；回看消息时不强制滚到底。运行中补充指令失败不再伪装已发送，保留输入供核对。

## 验收证据

| 检查 | 结果 | 边界 |
| --- | --- | --- |
| `npm run verify:local` | 通过：schema、TypeScript/Vite、回归、安全、审批、API 客户端、用量、准入、通知、个人文件/事实及安装器测试 | 使用临时服务与假 Codex，不计费 |
| `npm run verify:personal:ui` | 通过：空间/草稿、个人附件、服务连接、事实、今日到文件、用量逆序响应和刷新失败旧数据提示、7 个宽度无横向溢出、390px 对话高度、320px 文件页及 390×480 输入区可见 | 截图由脚本生成于 `docs/research/acceptance/personal-usage-2026-09-26/`；模拟数据 |
| `npm run verify:safety:ui` | 通过：跨项目文件、草稿/发送、流式中断/重连、失败的补充指令、Review 写操作和移动布局 | 浏览器自动化，不代表真机软键盘 |
| 个人资料 HTTP 回归 | 共享模式上传字节保留、预览与下载响应头/字节、个人外路径/符号链接拒绝、会话删除保留上传副本、引用中拒删、显式删除不碰产物、事实新增与删除影响新任务 | 未验证大文件吞吐与真实模型产物 |
| 专用 worker 文件通道 | 模拟 worker 对上传、列表、预览、删除、隐藏路径/符号链接拒绝及 socket 启动通过 | 未在 Linux/systemd 和真实账号下端到端验收 |
| 独立调用方参考客户端 | 假服务端验证令牌、幂等键、运行状态轮询和显式取消 | 未对真实服务触发付费自动化 |
| 事件游标与接口 | 旧记录补序号、重启后续增、80 条裁剪缺口、跨调用方权限和不返回原始事件正文均通过假服务端回归 | 不具备事务领取、持久 SSE 重放或第三方回执核对 |
| 代码 Review | 修复预览按钮被 iframe 挤出视口、最近文件手机竖排、个人页混入工作审批、失败补充指令误显示；专用 worker 的文件/Skill 请求改为项目定向，worker 不可用时不回退主进程读取个人文件；运行时禁止删除个人上传副本 | Linux/systemd 专用账号未实机验证；无法据此证明真实第三方副作用具备事务保证 |

## 对标项状态

| 编号 | 本轮状态 | 未完成的验收条件 |
| --- | --- | --- |
| R2-01 | 本地完成 | 生产时区/真实数据核对 |
| R2-02 | 浏览器模拟通过 | iOS/Android 真机键盘、辅助技术检查 |
| R2-03 | 本地完成 | 实际账号服务目录与授权恢复体验 |
| R2-04 | 部分完成 | 专用 worker 的 Linux 实机验收、产物版本/会话归属与真实模型产物核验 |
| R2-05 | 部分完成 | 跨重启的个人任务状态、失败恢复和首次真实任务成功率 |
| R2-06 | 部分完成 | 事件游标及裁剪同步已完成；事务接收、持久领取和外部动作回执核对仍未完成 |
| R2-07 | 部分完成 | 真实参考客户端的提交/轮询/取消/用量全链路；本地假服务端已通过 |
| R2-08 | 部分完成 | CLI 候选版本兼容矩阵与生产升级门禁 |
| R2-09 | 部分完成 | 排队下一条、取消队列及跨页面持久语义；已完成回看不抢滚动和补充失败保护 |
| R2-10 | 本地完成 | 真实任务使用及删除后历史边界由用户确认 |
| R2-11～13 | 未纳入本次低风险实现 | Routine、业务写操作预览、持久浏览器/语音需独立范围、费用和权限确认 |

本轮没有把“页面显示可调用服务”当成读写邮箱成功，也没有把已知 token 软门槛说成账单硬上限。部署前需备份状态和个人目录、检查进行中的任务，并在用户确认维护窗口后更新前后端；仅替换静态页面会使新接口不可用。
